name: PD Module Tests

on:
  push:
    branches: [main, develop]
    paths:
      - 'src/scitex/pd/**'
      - 'tests/scitex/pd/**'
      - '.github/workflows/test-pd.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'src/scitex/pd/**'
      - 'tests/scitex/pd/**'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run pd tests
        run: |
          PYTHONPATH=./src pytest tests/scitex/pd/ -v --tb=short 2>&1 | tee test-results.log
          tail -10 test-results.log
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pd-test-results
          path: test-results.log
          retention-days: 7
