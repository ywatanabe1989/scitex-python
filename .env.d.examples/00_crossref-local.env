# CrossRef Local Configuration
# Direction: NAS has database -> Local machine connects via SSH tunnel

# Mode: db (direct database), http (via API), auto (detect)
CROSSREF_LOCAL_MODE=auto

# HTTP API URL (for http mode)
CROSSREF_LOCAL_API_URL=http://localhost:31291

# Server settings
CROSSREF_LOCAL_HOST=0.0.0.0
CROSSREF_LOCAL_PORT=31291

# Path to SQLite database (for db mode)
# CROSSREF_LOCAL_DB=/path/to/crossref.db
